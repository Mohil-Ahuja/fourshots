[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "fourshots-rules"
version = "0.1.0"
description = "Differential-testing oracle for the fourshots constraint lattice."
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
